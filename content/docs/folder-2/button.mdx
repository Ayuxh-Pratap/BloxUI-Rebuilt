---
title: Buttons>
date: 2024-05-22
description: A button with a Shin.
author: ayush
published: true
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import {Callout} from "fumadocs-ui/components/callout";
import InfoCard2 from "components/InfoCard2";

## Squeezy Button
Button Component that Squeez with y-axis when `Clicked` or `Hovered`.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
  <InfoCard2 />
  </Tab>
  <Tab value="Code">
  ```typescript
  import SqueezyButton from "@/components/bloxui/squeezy-button";
 
export function SqueezyButtonDemo() {
  return (
    <div className="z-10 flex min-h-[16rem] items-center justify-center">
      <SqueezyButton className="">
          Button
      </SqueezyButton>
    </div>
  );
}
  ```
  </Tab>
</Tabs>

In this directory, there should be rolling daily logs:

```sh copy
components/bloxui/squeezy-button.tsx
```

```js filename="tailwind.config.js" copy
import React from 'react'
import Image from "next/image";

export default function InfoCard() {
    return (
      <div className="flex mt-20 justify-center items-center">
          <button className="cursor-pointer transition-all bg-blue-500 text-white px-6 py-2 rounded-lg border-blue-600 border-b-[4px] hover:brightness-110 hover:-translate-y-[1px] hover:border-b-[6px] active:border-b-[2px] active:brightness-90 active:translate-y-[2px]">
              Button
          </button>
      </div>
    )
}
```
<Callout type="info">
You can play with the Styling of Main "div" to Change Card Theme According to Your Websites's aesthetics
</Callout>

Extend `height` with new value and install plugins to work with image.